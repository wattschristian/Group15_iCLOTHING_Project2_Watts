@model IEnumerable<Group15_iCLOTHINGApp.Models.ItemDelivery>

@{
    ViewBag.Title = "Index";
}
@if (Session["UserID"] == null)
{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
else if (Session["AdminID"] != null)
{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
else
{
    Layout = "~/Views/Shared/_LayoutLoggedIn.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stickerDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.stickerDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.stickerID }) |
                @Html.ActionLink("Details", "Details", new { id = item.stickerID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.stickerID })
            </td>
        </tr>
    }

</table>
