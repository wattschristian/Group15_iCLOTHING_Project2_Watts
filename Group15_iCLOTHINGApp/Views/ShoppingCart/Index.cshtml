@model IEnumerable<Group15_iCLOTHINGApp.Models.ShoppingCart>

@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/Shared/_LayoutLoggedIn.cshtml";
    decimal totalPrice = 0;
    foreach (var item in Model)
    {
        totalPrice += item.cartProductPrice * item.cartProductQty;
    }
    ViewBag.CartTotal = totalPrice;
}

<!DOCTYPE html>
<style>
    /* Inline CSS for cart page */
    body {
        font-family: Arial, sans-serif; /* Match the font-family of the navbar */
        background-color: #f9f9f9;
        padding: 20px;
    }

    h2 {
        margin-bottom: 20px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        border-spacing: 0;
    }

    th, td {
        padding: 10px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    th {
        background: border-box; /* Match the navbar gradient */
        background-color: rgb(0, 136, 180);
        color: black;
    }

    tr:nth-child(even) {
        background-color: #f9f9f9;
    }

    a {
        display: inline-block;
        margin-bottom: 10px;
        padding: 5px 10px;
        background: linear-gradient(to right, #667eea, #764ba2); /* Match the navbar gradient */
        color: #fff;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s ease; /* Add smooth transition */
    }

    a:hover {
        background: linear-gradient(to right, #5a3ec8, #6a82fb); /* Darken the gradient on hover */
    }
</style>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - iCLOTHING Web Store</title>
</head>
<h2>@ViewBag.Title</h2>
<hr />
@Html.ActionLink("Back", "Index", "UserQuery")
<table class="table" cellspacing="5">
    <tr>
        <th>Name</th>
        <th>Quantity</th>
        <th>Price</th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr data-name="@item.cartProductName" data-price="@item.cartProductPrice" data-id="@item.productID">
            <td>
                @Html.DisplayFor(modelItem => item.cartProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cartProductQty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cartProductPrice)
            </td>
            <td>
                <input type="number" min="0" max="@item.cartProductQty" />
            </td>
            <td>
                <input type="button" value="Remove" onclick="removeItemsFromCart(this)" />
            </td>
        </tr>
    }
</table>
@using (Html.BeginForm("Checkout", "OrderStatus"))
{
    <div style="display: inline-block;">
        <p>Total: $@ViewBag.CartTotal</p>
    </div>
    <input type="submit" value="Check Out" style="float:right" />
}
<script>
    $(function () {
        window.location.reload();
    })
    function removeItemsFromCart(button) {
        var id = $(button).parent().parent().data("id");
        var price = $(button).parent().parent().data("price");
        var name = $(button).parent().parent().data("name");
        var qtyPickerIndex = $(button).parent().siblings().length - 1;
        var qty = $(button).parent().siblings()[qtyPickerIndex].children[0].value;
        var url = '@Url.Action("Index", "ShoppingCart")';
        $.ajax({
            type: "POST",
            url: url,
            data: { add_removeFlag: false, productPrice: price, productID: id, productName: name, quantity: qty },
            dataType: "json",
            cache: false
        });
    }
</script>
